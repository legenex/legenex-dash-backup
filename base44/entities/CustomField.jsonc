{
  "name": "CustomField",
  "type": "object",
  "properties": {
    "field_name": {
      "type": "string"
    },
    "label": {
      "type": "string"
    },
    "field_type": {
      "type": "string",
      "enum": [
        "string",
        "number",
        "boolean",
        "date",
        "dropdown",
        "Calculated",
        "system"
      ],
      "default": "string"
    },
    "source": {
      "type": "string",
      "enum": [
        "inbound",
        "hlr",
        "leadbyte"
      ],
      "default": "inbound"
    },
    "sample_value": {
      "type": "string"
    },
    "options": {
      "type": "string",
      "description": "JSON array of dropdown values (for system/dropdown fields e.g. lead_status). Drives the trigger options shown on Destinations and Conversion Events."
    },
    "include_in_leadbyte": {
      "type": "boolean",
      "default": false
    },
    "leadbyte_field_name": {
      "type": "string"
    },
    "auto_created": {
      "type": "boolean",
      "default": false
    },
    "sort_order": {
      "type": "number",
      "default": 0
    },
    "required": {
      "type": "boolean",
      "default": false,
      "description": "If true, missing this field queues the lead at the gate. System fields are never gated (they are system-populated)."
    },
    "system_role": {
      "type": "string",
      "enum": [
        "",
        "email_valid",
        "phone_verified"
      ],
      "default": "",
      "description": "Marker for system-populated fields. processLead locates the email_valid / phone_verified field by this role to resolve the configured field_name (editable label/name on the Custom Fields page)."
    },
    "migration_source_id": {
      "type": "string",
      "description": "Backup mirror only. The record id this row was copied from in the primary Legenex dashboard app. Used by the sync job to keep this app 1:1 with the primary. Not used by any dashboard logic."
    },
    "source_created_date": {
      "type": "string",
      "format": "date-time",
      "description": "Backup mirror only. The created_date this record has in the primary Legenex dashboard app. Base44 stamps its own created_date on insert, so this column carries the real event time and the app's read layer substitutes it for created_date."
    }
  },
  "required": [
    "field_name"
  ]
}
