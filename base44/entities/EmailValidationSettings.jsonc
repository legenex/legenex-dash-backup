{
  "name": "EmailValidationSettings",
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "default": true,
      "description": "When true, inbound emails are validated (format + DNS MX) and the result is written to the email_valid system field for forwarding."
    },
    "filter_suppliers": {
      "type": "string",
      "description": "JSON array of supplier names or SIDs to validate email for. Empty = all suppliers."
    },
    "filter_supplier_types": {
      "type": "string",
      "description": "JSON array of supplier types (Internal, External, Calls). Empty = all."
    },
    "filter_routes": {
      "type": "string",
      "description": "JSON array of lead_route keys (standard, direct, data, event, queue) to validate email on. Empty = standard, direct, data."
    },
    "migration_source_id": {
      "type": "string",
      "description": "Backup mirror only. The record id this row was copied from in the primary Legenex dashboard app. Used by the sync job to keep this app 1:1 with the primary. Not used by any dashboard logic."
    },
    "source_created_date": {
      "type": "string",
      "format": "date-time",
      "description": "Backup mirror only. The created_date this record has in the primary Legenex dashboard app. Base44 stamps its own created_date on insert, so this column carries the real event time and the app's read layer substitutes it for created_date."
    }
  },
  "required": []
}
