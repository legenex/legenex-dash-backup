{
  "name": "Vertical",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Vertical display name e.g. Motor Vehicle Accident, Workers Comp, Debt"
    },
    "code": {
      "type": "string",
      "description": "Short code matching the inbound payload 'vertical' field value used to route leads into delivery buckets"
    },
    "description": {
      "type": "string"
    },
    "active": {
      "type": "boolean",
      "default": true
    },
    "sort_order": {
      "type": "number",
      "default": 0
    },
    "migration_source_id": {
      "type": "string",
      "description": "Backup mirror only. The record id this row was copied from in the primary Legenex dashboard app. Used by the sync job to keep this app 1:1 with the primary. Not used by any dashboard logic."
    }
  },
  "required": [
    "name",
    "code"
  ]
}
